import {mkdir} from 'fs/promises';

// await mkdir("myfolder");
// await mkdir("myfolder/subfolder");

// await mkdir("myfolder/subfolder2", {recursive: true});


// await rm("myfolder/subfolder2", {recursive: true});

// await rm("myfolder", {recursive: true});

